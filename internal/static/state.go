package static

var (
	// ApplicationMode defines if this application should start up in production mode ('PROD') or not (anything else)
	ApplicationMode = "DEV"

	// ApplicationVersion defines the version to expose through the REST API
	ApplicationVersion = "DEV-local"
)
